# default www hostname

#hostname=aestesis.yi.org 

# activate ftp push (remove the following #)
# and unactivate the webserver
# can use only jpeg file

useftp=true
ftpsrv=ftp.theftp.org
ftplog=thelogin
ftppwd=thepassword
ftpfile=www/pushtest/test.jpg